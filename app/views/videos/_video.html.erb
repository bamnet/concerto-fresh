<div id="<%= dom_id video %>">
  <div>
    <iframe id="player" class="m-auto" type="text/html" width="600" height="480"
      src="https://www.youtube-nocookie.com/embed/<%= video.video_id %>?rel=0&iv_load_policy=3"
      allow="autoplay; encrypted-media" allowfullscreen
      frameborder="0">
    </iframe>
  <% elsif video.video_source == "vimeo" %>
    <iframe id="player" class="m-auto" type="text/html" width="600" height="480"
      src="https://player.vimeo.com/video/<%= video.video_id %>?autoplay=1&muted=1&loop=0"
      allow="autoplay; encrypted-media" allowfullscreen
      frameborder="0">
    </iframe>
  <% else %>
    <p>Unsupported video source</p>
  <% end %>

  <%= render "contents/show_details", content: video %>
</div>