<div class="min-h-screen bg-gray-50">
  <div class="max-w-4xl mx-auto">
    <!-- Page Header -->
    <div class="mb-4">
      <nav class="flex items-center space-x-2 text-sm text-gray-500 mb-4">
        <%= link_to "Content", contents_path, class: "hover:text-gray-700" %>
        <%= heroicon("chevron") %>
        <span class="text-gray-900 font-medium">Add Graphic</span>
      </nav>
      
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-2xl font-bold text-gray-900">Add Graphic Content</h1>
          <p class="text-gray-600 mt-1">Upload and configure a new graphic for display on your screens</p>
        </div>
      </div>
    </div>

    <!-- Form -->
    <%= render "form", graphic: @graphic %>
  </div>
</div>

<%= page_title "Add Graphic" %>
